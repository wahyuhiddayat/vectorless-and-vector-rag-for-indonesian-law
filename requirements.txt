# Shared LLM SDKs (3-vendor pipeline: OpenAI parser + GT judge,
# Anthropic GT annotator + parser judge, Vertex Gemini summary + OCR + retrieval)
python-dotenv>=1.0.0
openai>=1.60.0
anthropic>=0.40.0
google-genai>=1.0.0
google-auth>=2.30.0
huggingface-hub>=0.20.0
tqdm
brotli>=1.1.0

# Scraper
requests
beautifulsoup4

# Vectorless RAG (indexing + retrieval)
pymupdf>=1.26.0

# Vector RAG (retrieval)
qdrant-client
rank-bm25
sentence-transformers>=3.0.0

# GT typed-query validation
Sastrawi>=1.0.1
